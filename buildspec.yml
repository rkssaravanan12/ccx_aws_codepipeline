version: 0.2

env:
  variables:
     stage: "prod"
     config: "http://config.com"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
      npm install && npm install serverless -g
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      # - command
      # - command
  post_build:
    commands:
      sls deploy --stage ${stage}
#artifacts:
  #files:
    # - location
    # - location
  #discard-paths: yes
  #base-directory: location